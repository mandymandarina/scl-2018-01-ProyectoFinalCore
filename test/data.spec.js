describe('data', () => {
  it('debería exponer una función register', () => {
    assert.isFunction(register);
  });

  it('debería exponer función login', () => {
    assert.isFunction(login);
  });

  it('debería exponer función notification', () => {
    assert.isFunction(notification);
  });

  it('debería exponer función logOut', () => {
    assert.isFunction(logOut);
  });

  describe('register(rut, name)', () => {
    
    
  });

  describe('login(rut)', () => {
    
    
  });

  describe('notification()', () => {
    
    
  });

  describe('logOut()', () => {
    
    
  });
});
